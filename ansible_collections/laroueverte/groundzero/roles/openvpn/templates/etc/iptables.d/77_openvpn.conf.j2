
#Autoriser le port 945(Cluster control channel) pour les membres du cluster
{% for mariadb_cluster_member in mariadb_cluster_members %}
-A INPUT -i {{ansible_default_ipv4.alias}} -p tcp --dport 945 --source {{mariadb_cluster_member}} -j ACCEPT
{% endfor %}
